{% extends 'base.html.twig' %}

{% block title %}Utilisateur{% endblock %}

{% block body %}
    <h1>Utilisateur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ utilisateur.id }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ utilisateur.pseudo }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-main" href="{{ path('app_utilisateur_index') }}">back to list</a>

    {% if is_granted('POST_USER', utilisateur)  %}
        <a class="btn btn-main" href="{{ path('app_utilisateur_edit', {'id': utilisateur.id}) }}">edit</a>
    {% endif %}
    {{ include('utilisateur/_delete_form.html.twig') }}
{% endblock %}
